{
  "name": "qci-trade-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute qci-trade-lab --file=./schema.sql --local",
    "db:remote": "wrangler d1 execute qci-trade-lab --file=./schema.sql --remote"
  },
  "devDependencies": { "wrangler": "^4.20.0" }
}
